databases:
  - name: truestate-db
    databaseName: truestate
    postgresMajorVersion: 16
    region: singapore

services:
  # Backend (Django)
  - type: web
    name: truestate-backend
    runtime: python
    region: singapore
    rootDir: backend
    buildCommand: ./build.sh
    startCommand: gunicorn backend.wsgi
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: truestate-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        # Placeholder; after first deploy, change to the exact backend hostname Render assigns
        value: truestate-backend.onrender.com
      - key: CORS_ALLOWED_ORIGINS
        # Placeholder; after first deploy, change to the actual frontend hostname
        value: https://truestate-frontend.onrender.com

  # Frontend (Vite React static site)
  - type: web
    name: truestate-frontend
    runtime: static
    rootDir: frontend/frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        # Placeholder; after first deploy, change to actual backend hostname
        value: https://truestate-backend.onrender.com/api
